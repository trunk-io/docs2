---
title: "Appearance and terminal preferences"
description: "Customize the trunk desktop app and terminal on the current device."
groups: ["firewatch"]
---

{/* Generated in trunk-io/docs2 from apps/firewatch/docs in trunk-io/trunk-cloud. */}

Appearance and terminal preferences are stored on the current device.

## Appearance

Navigate to **Settings** → **Appearance** → **Theme** to:

- Select a built-in light or dark theme.
- Import a VS Code theme on desktop.
- Change the interface scale.
- Choose interface and monospace fonts.

The app warns when an imported theme produces insufficient contrast. Clear the
imported theme to return to the built-in palette.

<Frame caption="Customize the app theme, scale, and fonts.">
  ![The trunk app appearance
  settings.](/assets/trunk-workspaces/appearance-settings.png)
</Frame>

## Terminal

Navigate to **Settings** → **Appearance** → **Terminal** to:

- Change the terminal font size and colors.
- Move the terminal tab strip to the top or bottom.
- Require confirmation before closing a terminal tab.
- Reset terminal appearance to its defaults.

These preferences do not change the shell, packages, or files inside a workspace.
